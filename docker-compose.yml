---
name: homepage
services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    ports:
      - 100.117.63.60:81:3000
    volumes:
      - config:/app/config
    env_file:
      - .env
    labels:
      - "com.centurylinklabs.watchtower.scope=global"
    restart: always
volumes:
  config:
    driver: local
    driver_opts:
      type: none
      device: ./config
      o: bind